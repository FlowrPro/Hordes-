// Runtime configuration for the client
// Replace WS_URL with your Render-provided backend URL (wss://...).
// On Netlify you can generate this at build-time from an environment variable.
window.CONFIG = {
  WS_URL: 'wss://hordes-backend.onrender.com' // <-- change this before deploy
};
